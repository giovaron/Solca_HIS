UPDATE MOVIMIENTOS_ROLES
    SET ESTADO = 'N'
    WHERE ESTADO = 'C' AND ROLPGS_NUMERO = 4;

uPDATE ROLES_DE_PAGO 
       SET ESTADO='R',
       CMP_TIPO=NULL,
       CMP_CLAVE=NULL 
       WHERE NUMERO=4

UPDATE ROLES_de_PAGOS
      SET ESTADO = 'R',
      CMP_TIPO = NULL,
      CMP_CLAVE = NULL
      WHERE ESTADO = 'C' AND NUMERO = 3;